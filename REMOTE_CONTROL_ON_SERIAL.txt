#include <IRremote.h>
IRrecv ir(13);
decode_results result;
void setup() {
  Serial.begin(9600);
  Serial.println("Hello");
  ir.enableIRIn();
}

void loop() {
  if(ir.decode(&result)){                              
    Serial.println(result.value, HEX);
    ir.resume();
    delay(300);
  }
} 